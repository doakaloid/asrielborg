<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

    <title>AsrielBorg Panel</title>

    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet">
    <!-- JQuery UI CSS -->
    <link href="/css/jquery-ui.min.css" rel="stylesheet">
    <link href="/css/jquery-ui.structure.min.css" rel="stylesheet">

    <style>
        body {
            padding-top: 30px;
        }

        .spacing {
            padding-bottom: 10px;
        }

        .asriel-table {
            table-layout: fixed;
        }

        .asriel-table > * {
            overflow: hidden;
            overflow-x: scroll;
        }
    </style>

    <!-- Socket.io -->
    <script type="text/javascript" src='/scripts/socket.io-1.4.5.js'></script>

    <title>AsrielBorg Control Panel</title>

    <!-- Read cookie script -->
    <script type="text/javascript">
        function readCookie(name) {
            var nameEQ = name + "=";
            var ca = document.cookie.split(';');
            for (var i = 0; i < ca.length; i++) {
                var c = ca[i];
                while (c.charAt(0) == ' ') c = c.substring(1, c.length);
                if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length, c.length);
            }
            return null;
        }
    </script>
</head>
<body>

<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <a class="navbar-brand" href="#">AsrielBorg Web Panel</a>
    </div>
</nav>

<div class="container-fluid">
    <div class="row">
        <!-- Users -->
        <div class="col-md-3">
            <div class="page-header">
                <h2>Users</h2>
                <div class="panel panel-primary">
                    <!-- Default panel contents -->
                    <div class="panel-heading"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Users
                    </div>

                    <!-- Table -->
                    <table class="table table-striped table-bordered asriel-table">
                        <thead>
                        <tr>
                            <th>Username</th>
                            <th>IP Address</th>
                        </tr>
                        </thead>
                        <tbody id="user-table">
                        </tbody>
                    </table>
                </div>
            </div>
        </div><!-- /Users -->

        <div class="col-md-7">
            <div class="page-header">
                <h2>Controls</h2>
            </div>
            <div class="row">

                <div class="col-md-6">
                    <span class="label label-primary">Reply rate: <span id="replyRate-value"></span></span>

                    <div class="spacing"></div>

                    <div id="replyRate-slider" class="slider asriel-slider"></div>

                    <div class="spacing"></div>

                    <span class="label label-primary">Reply nick: <span id="replyNick-value"></span></span>
                    <div class="spacing"></div>
                    <div id="replyNick-slider" class="asriel-slider"></div>

                    <div class="spacing"></div>

                    <span class="label label-primary">Reply magic: <span id="replyMagic-value"></span></span>
                    <div class="spacing"></div>
                    <div id="replyMagic-slider" class="asriel-slider"></div>
                </div>

                <div class="col-md-6">
                    <div class="spacing"></div>

                    <div class="panel panel-primary">
                        <div class="panel-heading"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
                            Extra Options
                        </div>

                        <div class="panel-body">
                            <div class="checkbox">
                                <label>
                                    <input type="checkbox" value="" id="learning-check"> Learning
                                    <label for="learning-check" id="learning-label"></label>
                                </label>
                            </div>
                            <div class="checkbox">
                                <label>
                                    <input type="checkbox" value="" id="speaking-check"> Speaking
                                    <label for="speaking-check" id="speaking-label"></label>
                                </label>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

            <div class="spacing"></div>

            <div class="row">
                <div class="col-md-4">
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            <span class="glyphicon glyphicon-tags" aria-hidden="true"></span>
                            <span> Magic Words
                                    <div class="btn-group btn-group-xs pull-right">
                                        <button align="right" type="button" class="btn btn-default" data-toggle="modal"
                                                data-target="#magic-word-modal"><span class="glyphicon glyphicon-pencil"
                                                                                      aria-hidden="true"></span> New</button>
                                    </div>
                                </span>
                        </div>
                        <table class="table table-striped table-bordered asriel-table">
                            <tbody id="magic-words-table">
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="col-md-8">

                </div>
            </div>
        </div>
    </div>
</div>

<!-- Magic Word Modal -->
<div class="modal fade" id="magic-word-modal" tabindex="-1" role="dialog" aria-labelledby="magic-word-modal-label">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" aria-label="Close" data-dismiss="modal"><span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title" id="magic-word-modal-label">What is the magic word you would like to add?</h4>
            </div>
            <div class="modal-body">
                <div class="input-group input-group-lg">
                    <input type="text" class="form-control" placeholder="Enter a word" data-toggle="popover"
                           id="magic-word-input">
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary"
                        onClick="submitValue($('#magic-word-input'), 'magicWordsAdd')">Submit
                </button>
            </div>
        </div>
    </div>
</div>

<!-- JQuery -->
<script type='text/javascript' src="/scripts/jquery-3.1.0.min.js"></script>
<script type='text/javascript' src="/scripts/jquery-ui.min.js"></script>

<script type="text/javascript">
    "use strict";

    $("#magic-word-input").keypress(function (e) {
        if (e.which == 13) {
            submitValue($(this), 'magicWordsAdd');
        }
    });

    $("#magic-words-table")
            .on('mouseenter', 'td', function () {
                $(this).append("<span class='glyphicon glyphicon-remove pull-right x-button' aria-hidden=true></span>");
            })
            .on('mouseleave', 'td', function () {
                $('.x-button').remove();
            });

</script>

<script type="text/javascript">
    "use strict";
    let token = readCookie('token');

    //If there is no token, you must go back to the main page
    //to login again and get a new one.
    if (typeof token === 'null') {
        window.location.href = '/';
    }
    else {
        var socket = io.connect('', {
            query: 'token=' + token
        });

        socket.on('connect', function () {
            console.log("Connected to the server.");
        });

        socket.on('updateBotPanel', function (data) {
            console.log("Received data: " + JSON.stringify(data));
            if (data.replyRate) {
                $("#replyRate-value").text(data.replyRate);
                $("#replyRate-slider").slider({
                    range: "min",
                    min: 0,
                    max: 100,
                    value: data.replyRate,
                });
            }
            if (data.replyNick) {
                $("#replyNick-value").text(data.replyNick);
                $("#replyNick-slider").slider({
                    range: "min",
                    min: 0,
                    max: 100,
                    value: data.replyNick,
                });
            }
            if (data.replyMagic) {
                $("#replyMagic-value").text(data.replyMagic);
                $("#replyMagic-slider").slider({
                    range: "min",
                    min: 0,
                    max: 100,
                    value: data.replyMagic,
                });
            }
            if (typeof data.learning === 'boolean') {
                $("#learning-check").prop("checked", true);
                if (data.learning) {
                    $("#learning-label").html("<span class='label label-success'>ON</span>");
                } else {
                    $("#learning-label").html("<span class='label label-danger'>OFF</span>");
                }
            }
            if (typeof data.speaking === 'boolean') {
                $("#speaking-check").prop("checked", true);
                if (data.speaking) {
                    $("#speaking-label").html("<span class='label label-success'>ON</span>");
                } else {
                    $("#speaking-label").html("<span class='label label-danger'>OFF</span>");
                }
            }
            if (data.magicWords) {
                $("#magic-words-table").html("");
                data.magicWords.forEach(function (word) {
                    $("#magic-words-table").append(`<tr><td class='magic-word-entry'>${word}</td></tr>`);
                });
            }
        });

        socket.on('updateToken', function (data) {
            token = data.token;

            let date = new Date();
            date.setTime(date.getTime() + (12 * 60 * 60 * 1000));

            document.cookie = `token=${token}; expires=${date.toGMTString()}; path=/`
        });

        socket.on('error', function (data) {
            alert("Connection closed. " + (data.message));
            document.cookie = 'token=;Path=/;expires Thu, 01 Jan 1970 00:00:01 GMT;'
            window.location.href = '/';
        });

        socket.on('invalidToken', function (data) {
            alert("Token has expired. Please log in again.");
            document.cookie = 'token=;Path=/;expires Thu, 01 Jan 1970 00:00:01 GMT;'
            window.location.href = '/';
        });

        socket.on('connect_timeout', function () {
            alert('The connection has timed out.');
        });

        socket.on('registeredUserList', function (data) {
            let onlineUsers = data;
            $("#user-table").html(""); // Empty the user list to receive a new one.
            onlineUsers.forEach(function (user) {
                $("#user-table").append(`<tr><td>${user.nickname}</td><td>${user.ipAddress}</td></tr>`);
            });
        });

        socket.on('submitDataResult', function (data) {
            switch (data.location) {
                case 'magicWordInput':
                    if (data.success) {
                        $("#magic-word-modal").modal("hide");
                        $("#magic-word-input")
                                .val("");
                    } else {
                        $("#magic-word-input")
                                .popover({
                                    content: data.message,
                                    placement: 'right',
                                });
                    }
                    break;
                default:
                    break;
            }
        });
    }

    function submitValue(jSelector, inputType) {
        let req = {
            submitType: inputType,
            value: null,
        };
        switch (inputType) {
            case 'magicWordsAdd':
                let magicWord = jSelector.val();
                if (typeof magicWord === 'string') {
                    magicWord = magicWord.trim();

                    req.value = magicWord;
                }
                break;
            default:
                break;
        }

        socket.emit("submitData", req);
    }
</script>

<!-- Bootstrap -->
<script type="text/javascript" src="/scripts/bootstrap.min.js"></script>
<script type="text/javascript">
    $(function () {
        $('[data-toggle="popover"]').popover()
    })
</script>
</body>
</html>